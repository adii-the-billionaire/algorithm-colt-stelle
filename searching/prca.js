function binarySearch( arr, elem ) {
    var start = 0
    var end = arr.length - 1
    var middle = Math.floor( ( start + end ) / 2 )
    console.log( start,middle,end )
    //these are start end middle indexes of an array
    console.log( arr[ start ], arr[ middle ], arr[ end ] )
    console.log(`binary search is only applicable in sorted array`)
}
const mia = binarySearch( [ 1, 2, 3, 4, 5, 6 ], 5 )
//i'm below confidence of using while loop
while ( arr[ middle != elem ] ) {
    if(elm )
}